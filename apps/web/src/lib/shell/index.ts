/**
 * Shell components - Future @spine/ui candidates
 *
 * These components provide the structural layout primitives for
 * spine-based applications. They are designed to be:
 * - Composable (mix and match as needed)
 * - Accessible (follow ARIA patterns)
 * - Themeable (use CSS custom properties)
 */
export { default as AppShell } from './AppShell.svelte';
export { default as Header } from './Header.svelte';
export { default as Sidebar } from './Sidebar.svelte';
export { default as NavItem } from './NavItem.svelte';
export { default as StatusBar } from './StatusBar.svelte';
export { default as TopBar } from './TopBar.svelte';
export { default as NavRail } from './NavRail.svelte';
